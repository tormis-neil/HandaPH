# Task List: Fix Render Deployment Error

- [x] Update `Dockerfile` to add `libzip-dev` to `apt-get install` dependencies.
- [x] Update `Dockerfile` to add `zip` to `docker-php-ext-install`.
- [x] Commit and push the `Dockerfile` to trigger the Render deployment.
